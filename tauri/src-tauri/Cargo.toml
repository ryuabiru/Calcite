[package]
name = "calcite-tauri"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
calcite_rust = { path = "../../rust" }
rfd = "0.15"
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = [] }

[build-dependencies]
tauri-build = { version = "2", features = [] }
